# Telegram 图床配置模板
# 复制此文件为 .env 并填写实际值

# ===================== Telegram Bot 配置 =====================
# Telegram Bot Token (从 @BotFather 获取)
# 可选：也可通过管理后台 > Telegram 设置进行配置
# 环境变量优先级高于数据库配置
BOT_TOKEN=your_bot_token_here

# ===================== 安全配置（强烈建议设置） =====================
# Flask 会话密钥 (建议使用随机字符串，不设置则自动生成临时密钥)
SECRET_KEY=your_random_secret_key_here

# 管理员用户名
ADMIN_USERNAME=admin

# 管理员密码 (首次启动会自动生成，建议手动设置)
ADMIN_PASSWORD=your_secure_password_here

# ===================== 服务器配置 =====================
PORT=18793
HOST=0.0.0.0

# 允许的跨域来源 (生产环境建议设置具体域名)
# 多个域名用逗号分隔，如: https://example.com,https://www.example.com
ALLOWED_ORIGINS=*

# ===================== 数据库配置 =====================
# 默认使用当前目录下的 telegram_imagebed.db
DATABASE_PATH=./data/telegram_imagebed.db

# ===================== 日志配置 =====================
LOG_LEVEL=INFO
LOG_FILE=./data/telegram_imagebed.log

# ===================== 代理配置（可选） =====================
# 如部署环境需要代理才能访问 Telegram/Cloudflare API，可设置以下变量
# requests 库会自动读取这些标准环境变量
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890
# NO_PROXY=localhost,127.0.0.1,.local